Привет! Мы пишем плагин для QGIS на C++ (Qt + GDAL) для расчета индексов и классификации лесов (проект CAJANDER). 
Я веб-разработчик (Angular, .NET Core), перехожу на строгий C++ (умные указатели, STL).
Сейчас мы изолированно проектируем слой ДАННЫХ и МОДЕЛИ.

Текущая задача: Разработать архитектуру для [УКАЖИ ЧТО, например: структуры Rule и Biome / класса ConfigManager для работы с JSON].

Нам нужно спроектировать:
1. Заголовочные файлы (.h) со структурами данных.
2. Логику хранения данных в памяти (векторы, умные указатели).
3. [Если применимо] Логику сохранения/чтения этих структур в JSON через QJsonDocument.

Пожалуйста, предложи структуру классов без привязки к интерфейсу. Напиши чистый, типизированный C++ код с краткими комментариями, почему выбрано то или иное архитектурное решение. Начнем с обсуждения полей и типов данных.


Привет! Мы пишем плагин для QGIS на C++ (Qt + GDAL). Проект CAJANDER. 
Я реализую UI-слой по принципу атомарного дизайна. Сейчас мы создаем один конкретный изолированный компонент интерфейса.

Текущая задача: Создать виджет/диалог [УКАЖИ НАЗВАНИЕ, например: RuleEditorDialog для сборки формулы].

Контекст виджета:
- Он наследуется от [QDialog или QWidget].
- Должен содержать элементы: [ПЕРЕЧИСЛИ, например: комбобокс индексов, поле ввода коэффициента с валидатором].
- Должен открываться как [модальное окно / встраиваться в таблицу].

Пожалуйста, помоги спроектировать этот виджет:
1. Напиши структуру заголовочного файла (.h) с объявлением UI-элементов.
2. Опиши логику инициализации в .cpp (настройка Layout, ограничений для полей ввода).
3. Пока не пиши тяжелую бизнес-логику, сфокусируемся только на верстке интерфейса на Qt Widgets и валидации полей.


Привет! Мы пишем плагин для QGIS на C++ (Qt + GDAL). Проект CAJANDER.
У меня уже спроектирован UI-виджет [НАЗВАНИЕ] и фоновый вычислительный поток [НАЗВАНИЕ, например: QgsTask]. Мне нужно связать их между собой, избегая гонки потоков и блокировки UI.

Текущая задача: Спроектировать систему Сигналов и Слотов для обмена данными.

Что нужно реализовать:
1. Передачу данных из UI в поток при старте (входные параметры).
2. Передачу прогресса (0-100%) и текстовых логов из фонового потока в UI в реальном времени.
3. Безопасную остановку потока по кнопке "Отмена" (Cancellation Token паттерн в C++).

Пожалуйста, покажи пример объявления сигналов (signals:) в классе потока и соответствующих слотов (slots:) в классе UI, а также синтаксис их связывания через `connect()`. Объясни нюансы передачи кастомных структур через сигналы Qt (нужна ли регистрация типов метасистемы).


Привет! Я пишу плагин для QGIS на C++ в среде VSCode на Windows/Linux. 
Мой плагин успешно собирается как DLL, но сейчас мне нужно подключить внешние зависимости экосистемы QGIS.

Текущая задача: Подключить и настроить использование [УКАЖИ ЧТО, например: библиотеки GDAL для чтения растров / модулей QtXml].

Мой текущий стек сборки: [УКАЖИ, например: CMake / примитивный скрипт билдера].
Мне нужно понять:
1. Какие пути к заголовочным файлам (.h) из поставки QGIS/OSGeo4W мне нужно прописать.
2. Какие файлы библиотек (.lib/.dll или .so) нужно указать линкеру.
3. Как правильно написать инклюд в коде (например, #include <gdal_priv.h>), чтобы компилятор не ругался на "No such file or directory".

Помоги настроить конфигурационный файл сборки для этой конкретной задачи.
